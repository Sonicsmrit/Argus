.env
node_modules/
frontend/dist/
*.db
*.db.gz
data/opensanctions_targets.csv
scratch/temp_*.json
# country-stats.json IS committed: hosts with ephemeral disks (Render free
# tier) load the precomputed snapshot at boot instead of scanning 1M+ rows.
!data/country-stats.json
